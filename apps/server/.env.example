# Required - the server throws on startup if either is unset.
NODE_ENV=development
DATABASE_PATH=./db.sqlite

# Optional - shown here at their defaults, can be omitted entirely.
HOST=0.0.0.0
PORT=3000
